INSERT INTO ROLE VALUES (1, 'ADMIN');
INSERT INTO ROLE VALUES (2, 'USER');
INSERT INTO USER VALUES (999, 1,'test@test.com', 'LastName', 'Test', 'uzerPazz');
INSERT INTO USER_ROLE VALUES (999, 1);
commit;
    --select * from user;
    --select email, password, active from user where email='test@test.com';
    --select u.email, r.role from user u inner join user_role ur on(u.user_id=ur.user_id) inner join role r on(ur.role_id=r.role_id) where u.email='test@test.com';